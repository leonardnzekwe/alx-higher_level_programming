-- a script that lists all rows of the table first_table from the database hbtn_0c_0 in one's MySQL server
-- Retrieves all the rows in `first_table` table in the current database
SELECT * FROM `first_table`;
