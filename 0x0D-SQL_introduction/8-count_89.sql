-- a script that displays the number of records with id = 89 in the table first_table of the database hbtn_0c_0 in one's MySQL server
-- Retrieves the number of records with the id as 89 in the `first_table` table in the current database
SELECT COUNT(*) FROM `first_table` WHERE id = 89;
